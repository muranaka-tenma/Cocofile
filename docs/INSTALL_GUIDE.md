# CocoFile - インストールガイド

**バージョン**: v0.1.1 Alpha
**最終更新**: 2025年11月7日

---

## 📋 目次

1. [システム要件](#システム要件)
2. [Linux インストール](#linux-インストール)
3. [Windows インストール](#windows-インストール)
4. [macOS インストール](#macos-インストール)
5. [初回起動](#初回起動)
6. [アンインストール](#アンインストール)
7. [トラブルシューティング](#トラブルシューティング)

---

## システム要件

### 最小要件

| 項目 | 要件 |
|-----|------|
| **CPU** | x86_64 (64bit) |
| **メモリ** | 512MB以上 |
| **ストレージ** | 100MB以上の空き容量 |
| **OS** | Linux / Windows 10+ / macOS 10.15+ |

### 推奨環境

| 項目 | 推奨 |
|-----|------|
| **CPU** | デュアルコア以上 |
| **メモリ** | 2GB以上 |
| **ストレージ** | 1GB以上の空き容量 |

---

## Linux インストール

### 方法1: .deb パッケージ（Debian/Ubuntu）

**対応ディストリビューション**:
- Ubuntu 22.04以降
- Debian 12以降
- Linux Mint 21以降
- その他Debian系

**インストール手順**:

```bash
# 1. ダウンロード
wget https://github.com/muranaka-tenma/Cocofile/releases/download/v0.1.1-alpha/CocoFile_0.1.1_amd64.deb

# 2. インストール
sudo dpkg -i CocoFile_0.1.1_amd64.deb

# 3. 依存関係の解決（エラーが出た場合）
sudo apt-get install -f

# 4. 起動
cocofile
```

**特徴**:
- ✅ アプリケーションメニューに自動登録
- ✅ 依存関係の自動インストール
- ✅ システムパッケージとして管理
- ✅ サイズ: 5.3MB（最小）

---

### 方法2: AppImage（全ディストリビューション）

**対応ディストリビューション**:
- すべてのLinuxディストリビューション
- FUSE不要（新形式）

**インストール手順**:

```bash
# 1. ダウンロード
wget https://github.com/muranaka-tenma/Cocofile/releases/download/v0.1.1-alpha/CocoFile_0.1.1_amd64.AppImage

# 2. 実行権限を付与
chmod +x CocoFile_0.1.1_amd64.AppImage

# 3. 起動
./CocoFile_0.1.1_amd64.AppImage
```

**デスクトップ統合（オプション）**:

```bash
# アプリケーションメニューに登録
mkdir -p ~/.local/share/applications
cat > ~/.local/share/applications/cocofile.desktop <<EOF
[Desktop Entry]
Name=CocoFile
Exec=/path/to/CocoFile_0.1.1_amd64.AppImage
Icon=cocofile
Type=Application
Categories=Utility;FileTools;
EOF
```

**特徴**:
- ✅ ポータブル（依存関係バンドル）
- ✅ root権限不要
- ✅ どのディストリビューションでも動作
- ⚠️ サイズ: 78MB（大）

---

### 方法3: tar.gz ポータブル版

**インストール手順**:

```bash
# 1. ダウンロードと展開
wget https://github.com/muranaka-tenma/Cocofile/releases/download/v0.1.1-alpha/cocofile-v0.1.1-linux-x86_64.tar.gz
tar -xzf cocofile-v0.1.1-linux-x86_64.tar.gz
cd v0.1.1-linux-x86_64

# 2. 実行権限を付与
chmod +x cocofile
chmod +x python-analyzer-x86_64-unknown-linux-gnu

# 3. 起動
./cocofile
```

**特徴**:
- ✅ ポータブル
- ✅ root権限不要
- ✅ 手動での配置・管理が可能
- ⚠️ サイズ: 41MB（中）

---

## Windows インストール

### 方法1: MSI インストーラー（推奨）

**システム要件**:
- Windows 10以降（64bit）
- WebView2ランタイム（自動インストール）

**インストール手順**:

1. **ダウンロード**
   ```
   https://github.com/muranaka-tenma/Cocofile/releases/download/v0.1.1-alpha/CocoFile_0.1.1_x64_en-US.msi
   ```

2. **インストーラーを起動**
   - ダウンロードした`.msi`ファイルをダブルクリック
   - ユーザーアカウント制御（UAC）の警告が表示されたら「はい」をクリック

3. **インストールウィザード**
   - 「次へ」をクリック
   - インストール先を選択（デフォルト: `C:\Program Files\CocoFile\`）
   - 「インストール」をクリック

4. **起動**
   - スタートメニューから「CocoFile」を検索して起動
   - またはデスクトップショートカットから起動

**特徴**:
- ✅ スタートメニュー統合
- ✅ プログラムと機能から管理
- ✅ 自動アンインストール対応
- ✅ WebView2自動インストール

---

### 方法2: スタンドアロン実行ファイル

**インストール手順**:

1. **ダウンロード**
   ```
   https://github.com/muranaka-tenma/Cocofile/releases/download/v0.1.1-alpha/CocoFile.exe
   ```

2. **配置**
   - 任意のフォルダに配置（例: `C:\Tools\CocoFile\`）

3. **起動**
   - `CocoFile.exe`をダブルクリック

**特徴**:
- ✅ ポータブル
- ✅ インストール不要
- ⚠️ WebView2が別途必要な場合あり

---

## macOS インストール

### DMG インストーラー

**システム要件**:
- macOS 10.15 Catalina以降
- Intel Mac（x86_64）
- Apple Silicon（M1/M2/M3）は Rosetta 2 経由で動作

**インストール手順**:

1. **ダウンロード**
   ```
   https://github.com/muranaka-tenma/Cocofile/releases/download/v0.1.1-alpha/CocoFile_0.1.1_x64.dmg
   ```

2. **DMGをマウント**
   - ダウンロードした`.dmg`ファイルをダブルクリック

3. **アプリケーションフォルダにコピー**
   - `CocoFile.app`を「Applications」フォルダにドラッグ&ドロップ

4. **初回起動**
   - アプリケーションフォルダから`CocoFile`を起動
   - 「"CocoFile"は開発元が未確認のため開けません」と表示された場合：
     1. `CocoFile.app`を右クリック（または Control + クリック）
     2. 「開く」を選択
     3. 「開く」をクリック

**Gatekeeperの警告について**:
- CocoFileは現在Appleの開発者署名がありません
- 初回起動時のみ上記の手順が必要です
- 2回目以降は通常通り起動できます
- 正式リリース（v0.2.0）で署名対応予定

**特徴**:
- ✅ macOS標準のインストール方法
- ✅ アプリケーションフォルダに統合
- ⚠️ 開発者署名なし（Alpha版のため）

---

## 初回起動

### 1. ようこそ画面

初回起動時に設定ウィザードが表示されます。

### 2. 監視フォルダの設定

```
スキャンするフォルダを選択してください:
☐ ホームディレクトリ
☐ ドキュメント
☐ ダウンロード
☑ 手動で追加...
```

**推奨設定**:
- まずは小さなフォルダ（例: `~/Documents`の一部）で試す
- 動作確認後、必要なフォルダを追加

### 3. 初回スキャン

```
選択したフォルダをスキャンしています...
進捗: 42% (1,234 / 2,945 ファイル)
```

**スキャン時間の目安**:
- 1,000ファイル: 約30秒
- 10,000ファイル: 約5分
- 100,000ファイル: 約30分

### 4. 完了

```
スキャン完了！
- スキャンしたファイル: 2,945
- インデックス化: 1,832
- スキップ: 1,113（対応外形式）
```

---

## アンインストール

### Linux

#### .deb版
```bash
sudo apt-get remove cocofile
# または完全削除
sudo apt-get purge cocofile
```

#### AppImage版
```bash
# ファイルを削除するだけ
rm CocoFile_0.1.1_amd64.AppImage
```

#### tar.gz版
```bash
# 展開したフォルダを削除
rm -rf v0.1.1-linux-x86_64
```

**データファイルの削除**:
```bash
rm -rf ~/.config/CocoFile
```

---

### Windows

#### MSI版
1. 設定 → アプリ → アプリと機能
2. 「CocoFile」を検索
3. 「アンインストール」をクリック

#### スタンドアロン版
```
フォルダごと削除
```

**データファイルの削除**:
```
%APPDATA%\CocoFile
```

---

### macOS

1. アプリケーションフォルダから`CocoFile.app`をゴミ箱に移動
2. ゴミ箱を空にする

**データファイルの削除**:
```bash
rm -rf ~/Library/Application\ Support/CocoFile
```

---

## トラブルシューティング

### Linux

#### 問題: `.deb`インストール時に依存関係エラー

**エラーメッセージ**:
```
dpkg: dependency problems prevent configuration of cocofile
```

**解決方法**:
```bash
sudo apt-get install -f
```

#### 問題: AppImageが起動しない

**解決方法1**: 実行権限を確認
```bash
chmod +x CocoFile_0.1.1_amd64.AppImage
```

**解決方法2**: `--appimage-extract-and-run`オプション
```bash
./CocoFile_0.1.1_amd64.AppImage --appimage-extract-and-run
```

---

### Windows

#### 問題: 「WebView2が見つかりません」エラー

**解決方法**:
WebView2ランタイムをインストール
```
https://developer.microsoft.com/en-us/microsoft-edge/webview2/
```

#### 問題: 「このアプリはお使いのPCでは実行できません」

**原因**: 32bit Windowsでは動作しません

**解決方法**: 64bit Windowsが必要です

---

### macOS

#### 問題: 「開発元が未確認」で開けない

**解決方法**:
1. `CocoFile.app`を右クリック
2. 「開く」を選択
3. 「開く」をクリック

#### 問題: Apple Siliconで動作が遅い

**原因**: Rosetta 2経由で実行されています

**解決方法**:
- v0.2.0でUniversal Binary対応予定
- 現在は正常動作しますが、若干遅くなります

---

## 📞 サポート

### 問題が解決しない場合

[GitHub Issues](https://github.com/muranaka-tenma/Cocofile/issues)に報告してください。

**報告時に含めてください**:
1. OS環境（Linux/Windows/macOS、バージョン）
2. インストール方法（.deb/AppImage/MSI/DMG）
3. エラーメッセージ（スクリーンショット推奨）
4. 実行したコマンド
5. ログファイル

---

**最終更新**: 2025年11月7日
**バージョン**: v0.1.1 Alpha
**プロジェクト**: CocoFile（ココファイル）
